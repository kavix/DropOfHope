# LifeLink — Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# --- Database ---
DB_HOST=localhost
DB_NAME=lifelink_db
DB_USER=root
DB_PASS=

# --- App ---
# Set to 'production' on a live server to disable error output
APP_ENV=development
